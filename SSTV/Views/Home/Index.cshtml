@model SSTV.ViewModels.ViewModelHome

@{
    ViewBag.Title = "Programtablå";
}

<div class="container-fluid">
    <nav class="navbar navbar-light">
        <ul class="nav navbar-nav" id="sitenav">
            <li><a href="#"><span class="glyphicon glyphicon-chevron-left"></span></a></li>
            <li><a href="#">Idag</a></li>
            <li><a href="#">Imorgon</a></li>
            <li><a href="#">Mån 12/11</a></li>
            <li><a href="#">Tis 13/11</a></li>
            <li><a href="#">Ons 14/11</a></li>
            <li><a href="#">Tor 15/11</a></li>
            <li><a href="#">Fre 16/11</a></li>
            <li><a href="#"><span class="glyphicon glyphicon-chevron-right"></span></a></li>
        </ul>

        <form class="navbar-form navbar-left" autocomplete="off" id="topnavsearch">
            <div class="input-group">
                <input id="inputautocomplete" type="text" class="form-control" placeholder="Sök program..." name="search">
                <span class="input-group-btn">
                    <button class="btn btn-default" id="formButton" onclick="getProgramInfo();">
                        <i class="glyphicon glyphicon-search"></i>
                    </button>
                </span>
               </div>
        </form>

        <form class="navbar-form navbar-left" autocomplete="off">
            <div class="input-group">
                @Html.TextBox("SearchString", null, new { @class = "form-control" })
                <span class="input-group-btn">
                    <button class="btn btn-default">
                        <i class="glyphicon glyphicon-search"></i>
                    </button>
                </span>
            </div>
        </form>
    </nav>
</div>
@*<form>*@
@*@Html.TextBox("searchString")*@
@*<input type="submit" value="Sök kategori..." />*@
@*</form>*@

<div class="col-xs-12">
    <p id="programinfo"></p>
        <ul id="prog-info-ul"></ul>
</div>


@* Carousel *@
<div class="container" @*text-center*@>
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner" role="listbox">
            <div class="item active">
                <img src="~/Images/samycketbattre.jpg" alt="Så mycket bättre">
            </div>

            <div class="item">
                <img src="~/Images/aktuellt.jpg" alt="Aktuellt">
                @*<div class="carousel-caption">
                    </div>*@
            </div>
        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>


<div class="container-fluid">
    <div id="tvchannel">
        <div class="row">
            @foreach (var item in Model.TVChannels)
            {
                <div class="col-xs-12 col-sm-6 col-md-3">
                    @*<div id="imgDiv">
                            <a href="programpage.html">
                                <img src="Images/svt1.png" class="img-responsive" alt="SVT1">
                            </a>
                        </div>*@
                    <div id="channelheader">
                        <h4>@Html.DisplayFor(modelItem => item.Name)</h4>
                    </div>
                    <ul class="list-group">
                        @foreach (var program in item.TVPrograms)
                        {
                            <li class="list-group-item">
                                @Html.DisplayFor(modelItem => program.StartTime)
                                @Html.DisplayFor(modelItem => program.Title)

                            </li>
                        }
                    </ul>
                </div>
            }
        </div>
    </div>
</div>





@*"GAMLA GREJERNA"


    <div class="container text-center" id="tvchannellist">
        <form>
                <label>Sök på kategori</label>
                @Html.TextBox("searchString")
                <input type="submit" value="Sök" />
            </form>
        <div class="row">
            @foreach (var item in Model.TVChannels)
            {
                <div class="col-xs-12 col-sm-6 col-md-3" id="channelcolumns">
                    <div id="imgDiv">
                            <a href="programpage.html">
                                <img src="Images/svt1.png" class="img-responsive" alt="SVT1">
                            </a>
                        </div>
                    <div id="channelname">
                        @Html.DisplayFor(modelItem => item.Name)

                    </div>

                    <div class="list-group">
                        <ul id="programlist">
                            @foreach (var program in item.TVPrograms)
                            {
                                <li>
                                    <!-- Links -->
                                    @Html.DisplayFor(modelItem => program.StartTime)
                                    @Html.DisplayFor(modelItem => program.Title)
                                    <br />
                                </li>

                                <li>
                                        @Html.DisplayFor(modelItem => program.Informations)
                                    </li>
                                    <li>
                                        @Html.DisplayFor(modelItem => program.EndTime)
                                    </li>
                                    <li>
                                        @foreach (var category in program.Categories)
                                        {
                                            @Html.DisplayName(category.Name) <br />
                                        }
                                    </li>
                            }
                        </ul>
                    </div>
                </div>
            }
        </div>
    </div>*@
